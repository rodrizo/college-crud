@model IEnumerable<CRUDColegio.Models.AlumnoGrado>

@{
    ViewData["Title"] = "Lista de alumnos y grados";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">

    @if(TempData["mensaje"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            @TempData["mensaje"]
            <button type="button" class="close" date-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }

    <div class="row">

        <div class="col-sm-6">
            <h3>Lista de alumnos y grados</h3>
        </div>

        <div class="col-sm-6">
            <a class="btn btn-success" d-block asp-action="Create" asp-controller="AlumnoGrado">Agregar nuevo alumno y grado</a>
        </div>
    </div>

    <div class="row">
        
        @if (Model.Count() > 0)
        {
            <table class="table table-bordered table table-striped">
                <thead>
                    <td>@Html.DisplayNameFor(m => m.Id)</td>
                    <td>@Html.DisplayNameFor(m => m.AlumnoId)</td>
                    <td>@Html.DisplayNameFor(m => m.GradoId)</td>
                    <td>@Html.DisplayNameFor(m => m.Seccion)</td>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.AlumnoId</td>
                            <td>@item.GradoId</td>
                            <td>@item.Seccion</td>
                            <td>
                                <a asp-controller="AlumnoGrado" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Editar</a>
                                <a asp-controller="AlumnoGrado" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                </tbody>


            </table>
        }
        else
        {
            <p>No hay registros</p>
        }

    </div>




</div>

